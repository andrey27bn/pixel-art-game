
## 🎮 Pixel Art Game

Добро пожаловать в **Pixel Art Game** — небольшой приключенческий проект в стиле ретро, вдохновленный классическими 2D-ролевыми играми с видом сверху.

Этот репозиторий демонстрирует ключевые основы геймплея: **перемещение на основе сетки (Grid-based movement)**, обработку пользовательского ввода и базовое **обнаружение столкновений** с препятствиями.

-----

### 🚀 Поиграть сейчас

Вы можете мгновенно попробовать текущую версию игры, перейдя по ссылке:
👉 **[Pixel Art Game - Демо](https://pixel-art-game-rouge.vercel.app/)**

-----

### ✨ Ключевые Особенности

  * **Классический Вид Сверху:** Исследование мира в традиционном стиле, типичном для старых консольных RPG.
  * **Плавное Движение (Smooth Movement):** Персонаж движется от одной клетки сетки к другой с плавной, покадровой анимацией, сохраняя приятное ретро-ощущение.
  * **Карта Столкновений (Collision Map):** Используется невидимый массив данных (`collisionLayer` в `world.js`), который эффективно предотвращает прохождение героя через препятствия (стены, деревья, вода).
  * **Интерактивный Дебаг-Режим:** Нажмите клавишу **$\text{Enter}$** или **$\text{Пробел}$** во время игры, чтобы переключить режим отладки и визуализировать игровую сетку, границы объектов и заблокированные тайлы (столкновения).

-----

### 🕹️ Управление

Управление персонажем интуитивно понятно и поддерживает обе популярные раскладки:

| Действие | Клавиши |
| :--- | :--- |
| **Движение Вверх** | $\text{W}$ / $\uparrow$ |
| **Движение Вниз** | $\text{S}$ / $\downarrow$ |
| **Движение Влево** | $\text{A}$ / $\leftarrow$ |
| **Движение Вправо** | $\text{D}$ / $\rightarrow$ |
| **Переключить Отладку** | $\text{Enter}$ / $\text{Пробел}$ |

-----

### ⚙️ Технологии

Проект разработан с фокусом на простоту, производительность и модульность, используя нативный Web API.

| Категория | Технология | Описание |
| :--- | :--- | :--- |
| **Язык** | **JavaScript** (ES Modules) | Чистый, современный синтаксис. |
| **Отрисовка** | **HTML5 Canvas API** | Высокопроизводительная отрисовка 2D-графики. |
| **Структура** | **Модульный Дизайн** | Логика четко разделена на модули (`.js` файлы) для чистоты кода. |

-----

### 🏗️ Структура Проекта

Проект организован в виде модулей, каждый из которых отвечает за свой, хорошо определенный аспект игры:

| Файл | Описание |
| :--- | :--- |
| `main.js` | **Точка входа.** Инициализирует игру (`Game` class), настраивает Canvas, определяет константы (`TILE_SIZE`) и содержит **главный цикл игры** (`animate`). |
| `hero.js` | Определяет класс `Hero`. Содержит логику движения героя (вычисление следующей позиции), обработку ввода, анимацию спрайтов и проверку столкновений (`world.getTile`). |
| `gameObject.js` | **Базовый класс** для всех игровых объектов. Предоставляет общие свойства и ключевой метод `moveTowards` для реализации плавного перемещения. |
| `world.js` | **Менеджер игрового мира.** Управляет данными уровня (слои тайлов, карта столкновений) и методами для отрисовки фона, переднего плана и дебаг-слоев. |
| `input.js` | Обрабатывает события нажатия и отпускания клавиш. Управляет очередью ввода и предоставляет доступ к последнему активному направлению движения. |
| `index.html` | Основной HTML-файл. Включает `<canvas>` элемент и теги для предварительной загрузки всех ассетов (изображений) перед запуском основного скрипта. |

-----

### 🛠️ Запуск Проекта Локально

Для того, чтобы запустить игру на вашем компьютере, вам потребуется локальный веб-сервер, так как модули JavaScript (ES Modules) не могут быть корректно загружены напрямую из файловой системы браузера.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/andrey27bn/pixel-art-game.git
    cd pixel-art-game
    ```
2.  **Запустите локальный сервер:**
      * *Рекомендация:* Используйте расширение **Live Server** в VS Code.
      * *Альтернатива:* Используйте команду $\text{python -m http.server}$ или любой другой простой HTTP-сервер.
3.  **Откройте `index.html`** через запущенный локальный сервер.


-----

Что бы вы хотели отредактировать в следующей версии (например, добавить раздел "Вклад" или "Скриншоты")?
